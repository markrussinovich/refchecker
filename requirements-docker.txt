# Docker requirements - standard image (no GPU/vLLM dependencies)
# This is a subset of requirements.txt optimized for smaller image size

# Core dependencies - required for basic functionality
numpy<2  # Pin to NumPy 1.x for compatibility with compiled modules
requests>=2.25.0
beautifulsoup4>=4.9.0
pypdf>=5.0.0
arxiv>=1.4.0
python-dateutil>=2.8.0
tqdm>=4.60.0
colorama>=0.4.4
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.12.0
cryptography>=42.0.0  # For API key encryption in web UI
bibtexparser>=1.4.0  # For parsing ArXiv BibTeX citations

# Additional core dependencies found in codebase
pandas>=1.3.0
pdfplumber>=0.6.0
pymupdf>=1.23.0  # For PDF thumbnail generation

# Web UI backend dependencies
fastapi>=0.100.0
uvicorn[standard]>=0.22.0  # Includes websockets and other production dependencies
pydantic>=2.0.0
aiosqlite>=0.19.0
httpx>=0.24.0
Pillow>=9.0.0  # For thumbnail image enhancement
python-multipart>=0.0.6  # For FastAPI form data handling

# LLM provider dependencies (API-based, lightweight)
openai>=1.0.0
anthropic>=0.7.0
google-generativeai>=0.3.0

# Optional enhanced functionality
lxml>=4.6.0

# Note: Heavy dependencies excluded for smaller image:
# - torch, vllm (local model inference - use Dockerfile.gpu)
# - selenium (browser automation)
# - scikit-learn, nltk, joblib (ML features)
