# Development/CI dependencies - minimal set for running tests
# Use this for testing and CI to avoid disk space issues

# Core dependencies - required for basic functionality
numpy>=2.4.3  # NumPy 2.x
requests>=2.32.5
beautifulsoup4>=4.14.3
pypdf>=6.9.1
arxiv>=2.4.1
python-dateutil>=2.9.0.post0
tqdm>=4.67.3
colorama>=0.4.4
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.27.3
pandas>=3.0.1
pdfplumber>=0.11.9

# Web UI / auth dependencies (needed for backend/auth.py tests)
python-jose[cryptography]>=3.5.0
httpx>=0.28.1
fastapi>=0.135.1
pydantic>=2.12.5
anthropic>=0.86.0

# Testing dependencies
pytest>=9.0.2
pytest-cov>=7.0.0

# Note: The following are NOT included to save disk space in CI:
# - openai, google-generativeai (LLM providers - imported lazily)
# - torch, vllm (local model inference - very large)
# - scikit-learn, nltk, joblib (optional ML features)
# - black, isort, flake8, mypy (linting - not needed for tests)
# - lxml, pikepdf, selenium (optional parsing features)
# - huggingface_hub (HuggingFace API)
# Anthropic is included because the live hallucination fixture test exercises
# the provider path when ANTHROPIC_API_KEY is present.
#
# Install full requirements.txt if you need these features
